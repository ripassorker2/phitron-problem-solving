#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int h, a, b;
        cin >> h >> a >> b;

        if (b > a)
            cout << "Impossible\n";
        else
        {
                }
    }

    cout << endl;
    return 0;
}