#include <bits/stdc++.h>
using namespace std;
#define lli long long int

int main()
{
    lli A, B, C, D, X;

    cin >> A >> B >> C >> D;

    X = (A * B) - (C * D);

    cout << "Difference = " << X << endl;

    cout << endl;
    return 0;
}